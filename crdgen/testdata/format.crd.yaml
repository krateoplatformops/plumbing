---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: hellos.test.krateo.io
spec:
  group: test.krateo.io
  names:
    categories:
    - krateo
    - test
    - hello
    kind: Hello
    listKind: HelloList
    plural: hellos
    singular: hello
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v0-0-0
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              createdAt:
                description: Timestamp when the greeting was created
                example:
                - "2025-11-12T10:30:00Z"
                format: date-time
                type: string
              display:
                default: 1
                description: Number of times the message should be displayed
                example:
                - "1"
                - "3"
                format: int32
                type: integer
              greeting:
                description: The text of the greeting message
                example:
                - Hello, world!
                - Hi there!
                type: string
              ipAddress:
                description: The sender’s IPv4 address
                example:
                - 192.168.1.10
                format: ipv4
                type: string
              senderEmail:
                description: Email address of the sender
                example:
                - hello@krateo.io
                format: email
                type: string
              url:
                default: https://krateo.io
                description: A link associated with the greeting message
                format: uri
                type: string
              verbose:
                default: false
                description: If true, show additional information
                type: boolean
            required:
            - greeting
            - senderEmail
            type: object
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
